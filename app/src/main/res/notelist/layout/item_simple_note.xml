<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:background="?android:attr/selectableItemBackground">

    <TextView
        style="@style/ItemTitleTextView"
        android:id="@+id/tv_title"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        tools:text="title"/>

    <TextView
        style="@style/ItemContentTextView"
        android:id="@+id/tv_content"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        tools:text="content"/>

    <TextView
        style="@style/ItemDateTextView"
        android:id="@+id/tv_date"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        tools:text="12.12.2012"/>

    <!-- SPLITTER -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="@dimen/md_padding_half"
        android:layout_marginBottom="@dimen/md_padding_half"
        android:background="@color/clLightGrey"/>
</LinearLayout>